// This file is kept for compatibility but doesn't store any data
// since the website is a static portfolio with no database requirements

// Simple empty storage class for compatibility if needed
export class Storage {
  constructor() {
    // No initialization needed
  }
}

export const storage = new Storage();
